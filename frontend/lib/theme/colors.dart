import 'package:flutter/material.dart';

/// Central color palette for the SgBe Vision app.
/// All high-contrast colors are defined here for accessibility.
class AppColors {
  // Primary palette
  static const Color primaryBlue = Color(0xFF1565C0);
  static const Color primaryDark = Color(0xFF0D47A1);

  // Accent colors
  static const Color accentOrange = Color(0xFFFF8F00);
  static const Color accentGreen = Color(0xFF2E7D32);
  static const Color accentRed = Color(0xFFC62828);

  // Neutral colors
  static const Color pureWhite = Color(0xFFFFFFFF);
  static const Color pureBlack = Color(0xFF000000);

  // Surface colors
  static const Color darkSurface = Color(0xFF121212);
  static const Color darkCard = Color(0xFF1E1E1E);
  static const Color lightGray = Color(0xFFB0B0B0);

  // Semantic colors
  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFFF8F00);
  static const Color error = Color(0xFFC62828);
  static const Color info = Color(0xFF1565C0);
}
